{
  "name": "chocoapp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "ionic build",
    "start": "npx serve -s www -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.5"
  },
  "devDependencies": {
    "@angular/cli": "^18.0.0",
    "@ionic/cli": "^7.0.0"
  }
}
